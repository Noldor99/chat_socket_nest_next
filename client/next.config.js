/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "https://chat-back-yvv3.onrender.com",
      "loremflickr.com",
    ],
  },
}

module.exports = nextConfig
